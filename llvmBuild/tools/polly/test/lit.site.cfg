## Autogenerated by LLVM/Polly configuration.
# Do not edit!
config.llvm_src_root = "D:/GithubRepos/KoseligLang/llvm"
config.llvm_obj_root = "D:/GithubRepos/KoseligLang/llvmBuild"
config.llvm_tools_dir = "D:/GithubRepos/KoseligLang/llvmBuild/%(build_mode)s/bin"
config.llvm_libs_dir = "D:/GithubRepos/KoseligLang/llvmBuild/%(build_mode)s/lib"
config.polly_obj_root = "D:/GithubRepos/KoseligLang/llvmBuild/tools/polly"
config.polly_lib_dir = "D:/GithubRepos/KoseligLang/llvmBuild/tools/polly/lib"
config.target_triple = "i686-pc-win32"
config.enable_gpgpu_codegen = "FALSE"
config.link_polly_into_tools = "ON"
config.targets_to_build = " AArch64 AMDGPU ARM BPF Hexagon Lanai Mips MSP430 NVPTX PowerPC RISCV Sparc SystemZ X86 XCore"
config.extra_paths = "".split(";")

## Check the current platform with regex
import re
EAT_ERR_ON_X86 = ' '
if (re.match(r'^x86_64*', 'i686-pc-win32') == None) :
  EAT_ERR_ON_X86 = '|| echo \"error is eaten\"'

for arch in config.targets_to_build.split():
    config.available_features.add(arch.lower() + '-registered-target')

# Support substitution of the tools and libs dirs with user parameters. This is
# used when we can't determine the tool dir at configuration time.
try:
    config.llvm_tools_dir = config.llvm_tools_dir % lit_config.params
    config.llvm_libs_dir = config.llvm_libs_dir % lit_config.params
except KeyError:
    e = sys.exc_info()[1]
    key, = e.args
    lit_config.fatal("unable to find %r parameter, use '--param=%s=VALUE'" % (key,key))

if config.link_polly_into_tools == '' or \
   config.link_polly_into_tools.lower() == '0' or \
   config.link_polly_into_tools.lower() == 'n' or \
   config.link_polly_into_tools.lower() == 'no' or \
   config.link_polly_into_tools.lower() == 'off' or \
   config.link_polly_into_tools.lower() == 'false' or \
   config.link_polly_into_tools.lower() == 'notfound' or \
   config.link_polly_into_tools.lower() == 'link_polly_into_tools-notfound':
    config.substitutions.append(('%loadPolly', '-load '
                                 + config.polly_lib_dir + '/LLVMPolly.dll'
                                 + ' -polly-process-unprofitable '
                                 + ' -polly-remarks-minimal '
                                 + ' -polly-use-llvm-names '
                                 + ' -polly-import-jscop-dir=%S '
                                 ))
else:
    config.substitutions.append(('%loadPolly', ''
                                 + ' -polly-process-unprofitable '
                                 + ' -polly-remarks-minimal '
                                 + ' -polly-use-llvm-names '
                                 + ' -polly-import-jscop-dir=%S '
                                 ))

if config.enable_gpgpu_codegen == 'TRUE' :
    config.available_features.add('pollyacc')

# Let the main config do the real work.
lit_config.load_config(config, "D:/GithubRepos/KoseligLang/llvm/tools/polly/test/lit.cfg")
